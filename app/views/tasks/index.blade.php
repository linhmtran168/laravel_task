@extends('layouts.master')@section('content')<div class="row">  <div class="col-md-3">    <a href="{{{ route('create_task_path') }}}" class="btn btn-primary">Create New Task</a>  </div></div><div class="row task-list">  <div class="cold-md-12">    @include('flash::message')    <table class="table table-striped table-bordered table-hover">    <tr>      <th>Task Name</th>      <th>Importance</th>      <th>Status</th>      <td>Action</th>    </tr>    @foreach ($tasks as $task)    <tr>      <td>{{{ $task->name }}}</td>      <td>{{{ ucfirst($task->importance) }}}</td>      <td>{{{ ucwords(str_replace("_", " ", $task->status)) }}}</td>      <td>        <a href="{{{ route('edit_task_path', $task->id) }}}" class="btn btn-warning edit-task-butt">Edit</a>        {{ Form::open(['route' => ['delete_task_path', $task->id], 'id' => '_form', 'name' => 'createTaskForm', 'class' => 'delete-task-form']) }}        <button type="submit" class="btn btn-danger">Delete</button>        {{ Form::close() }}      </td>    </tr>    @endforeach    </table>    {{ $tasks->links() }}  </div></div@stop